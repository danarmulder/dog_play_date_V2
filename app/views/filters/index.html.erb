Suggestions for
<% current_user.dogs.each {|dog| "#{dog.name}, "} %>
<% @dogs.each do |dog| %>
  <%= dog.name %>
<% end %>

<h4>PREFERENCES</h4>
<ul>
  <% @filters.each do |filter| %>
    <% if filter.id != nil %>
      <li><%= filter.type %> Preference: <%= filter.content %>
      <%= link_to "Edit Preference", edit_filter_path(filter), id: "edit-filter-#{filter.id}" %>
      <%= link_to "Delete Preference", filter_path(filter.id), method: :delete, data: {confirm: "Are you sure you want to delete this preference?"}, id: "delete-filter-#{filter.id}-action" %>
      </li>
    <% end %>
  <% end %>
</ul>

<%= form_for([@user, @filter]) do |f| %>
  <input id="new-filter-type" name="filter[type]" type="hidden" value="Size">
  <%= f.label "Size Preference" %>
  <%= f.text_field :content, id: "size-field" %>
  <%= f.submit "Submit Size Preference", id: 'size-submit-filters' %>
<% end %>

<%= form_for([@user, @filter]) do |f| %>
  <input id="new-filter-type" name="filter[type]" type="hidden" value="Gender">
  <%= f.label "Gender Preference" %>
  <%= f.text_field :content, id: "gender-field" %>
  <%= f.submit "Submit Size Preference", id: 'gender-submit-filters' %>
<% end %>

<%= form_for([@user, @filter]) do |f| %>
  <input id="new-filter-type" name="filter[type]" type="hidden" value="Personality">
  <%= f.label "Personality Preference" %>
  <%= f.select :content, Personality.options.map {|opt| opt.humanize}, id: "personality-field" %>
  <%= f.submit "Submit Personality Preference", id: 'personality-submit-filters' %>
<% end %>
